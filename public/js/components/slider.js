const slider=document.querySelector(".slider-images"),sliderImages=document.querySelectorAll(".slide"),prevBtn=document.querySelector(".prev"),nextBtn=document.querySelector(".next"),currentSlideDisplay=document.querySelector(".currentSlideDisplay"),totalSlideDisplay=document.querySelector(".totalSlideDisplay");let currentSlide=0;prevBtn.onclick=()=>{currentSlide--,-1==currentSlide?(currentSlide=sliderImages.length-1,slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`):(slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`)},nextBtn.onclick=()=>{currentSlide++,currentSlide==sliderImages.length?(currentSlide=0,slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`):(slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`)},setInterval((()=>{currentSlide++,currentSlide==sliderImages.length?(currentSlide=0,slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`):(slider.style.transform=`translateX(${100*-currentSlide}%)`,currentSlideDisplay.innerHTML=`${currentSlide+1}`)}),3e3),currentSlideDisplay.innerHTML=`${currentSlide+1}`,totalSlideDisplay.innerHTML=`${sliderImages.length}`;
//# sourceMappingURL=slider.js.map